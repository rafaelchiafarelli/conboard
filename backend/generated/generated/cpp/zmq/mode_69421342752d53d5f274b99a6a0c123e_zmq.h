// Generated by harpia Stage 13 (ZMQ transport). Do not edit.
#ifndef HARPIA_ZMQ_MODE_69421342752d53d5f274b99a6a0c123e
#define HARPIA_ZMQ_MODE_69421342752d53d5f274b99a6a0c123e

#include <cstring>
#include <string>
#include <zmq.hpp>
#include "protofiles/mode_69421342752d53d5f274b99a6a0c123e.pb.h"

namespace harpia {
namespace zmq_transport {

// push/pull: mode_sender pushes (stamping origin), mode_receiver pulls.
class mode_sender {
public:
    mode_sender(::zmq::context_t& ctx, const std::string& endpoint)
        : mode_sender(ctx, endpoint, origin_id()) {}
    // many-to-*: the zmq/socket module assigns the sender id at runtime
    mode_sender(::zmq::context_t& ctx, const std::string& endpoint,
                  const std::string& origin)
        : origin_(origin), socket_(ctx, ::zmq::socket_type::push) {
        socket_.connect(endpoint);
    }
    // compile-time unique sender number (one-to-*): hash(file)+message
    static const std::string& origin_id() {
        static const std::string id = "305330307985827172";
        return id;
    }
    const std::string& origin() const { return origin_; }
    bool send(const ::mode& msg) {
        ::mode stamped = msg;
        stamped.set_originator_69421342752d53d5f274b99a6a0c123e(origin_);
        std::string bytes;
        if (!stamped.SerializeToString(&bytes)) return false;
        ::zmq::message_t frame(bytes.size());
        std::memcpy(frame.data(), bytes.data(), bytes.size());
        return socket_.send(frame, ::zmq::send_flags::none).has_value();
    }
    ::zmq::socket_t& socket() { return socket_; }
private:
    std::string origin_;
    ::zmq::socket_t socket_;
};

class mode_receiver {
public:
    mode_receiver(::zmq::context_t& ctx, const std::string& endpoint)
        : socket_(ctx, ::zmq::socket_type::pull) { socket_.bind(endpoint); }
    bool recv(::mode* msg) {
        ::zmq::message_t frame;
        if (!socket_.recv(frame, ::zmq::recv_flags::none).has_value()) return false;
        return msg->ParseFromArray(frame.data(), static_cast<int>(frame.size()));
    }
    ::zmq::socket_t& socket() { return socket_; }
private:
    ::zmq::socket_t socket_;
};

// pub/sub (streaming/event): mode_publisher publishes (stamping origin), mode_subscriber receives.
class mode_publisher {
public:
    mode_publisher(::zmq::context_t& ctx, const std::string& endpoint)
        : mode_publisher(ctx, endpoint, origin_id()) {}
    // many-to-*: the zmq/socket module assigns the sender id at runtime
    mode_publisher(::zmq::context_t& ctx, const std::string& endpoint,
                  const std::string& origin)
        : origin_(origin), socket_(ctx, ::zmq::socket_type::pub) {
        socket_.bind(endpoint);
    }
    // compile-time unique sender number (one-to-*): hash(file)+message
    static const std::string& origin_id() {
        static const std::string id = "305330307985827172";
        return id;
    }
    const std::string& origin() const { return origin_; }
    bool publish(const ::mode& msg) {
        ::mode stamped = msg;
        stamped.set_originator_69421342752d53d5f274b99a6a0c123e(origin_);
        std::string bytes;
        if (!stamped.SerializeToString(&bytes)) return false;
        ::zmq::message_t frame(bytes.size());
        std::memcpy(frame.data(), bytes.data(), bytes.size());
        return socket_.send(frame, ::zmq::send_flags::none).has_value();
    }
    ::zmq::socket_t& socket() { return socket_; }
private:
    std::string origin_;
    ::zmq::socket_t socket_;
};

class mode_subscriber {
public:
    mode_subscriber(::zmq::context_t& ctx, const std::string& endpoint)
        : socket_(ctx, ::zmq::socket_type::sub) { socket_.connect(endpoint);
        socket_.set(::zmq::sockopt::subscribe, ""); }
    bool receive(::mode* msg) {
        ::zmq::message_t frame;
        if (!socket_.recv(frame, ::zmq::recv_flags::none).has_value()) return false;
        return msg->ParseFromArray(frame.data(), static_cast<int>(frame.size()));
    }
    ::zmq::socket_t& socket() { return socket_; }
private:
    ::zmq::socket_t socket_;
};

}  // namespace zmq_transport
}  // namespace harpia

#endif  // HARPIA_ZMQ_MODE_69421342752d53d5f274b99a6a0c123e
